{
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "cd js && npm install",
    "serve": "npm run postinstall && ./node_modules/.bin/http-server -a localhost -p 3000 -c-1"
  }
}
